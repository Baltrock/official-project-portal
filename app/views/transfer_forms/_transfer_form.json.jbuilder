json.extract! transfer_form, :id, :transfer_from, :transfer_to, :project_involved, :transfer_reason, :outcome_expectation, :created_at, :updated_at
json.url transfer_form_url(transfer_form, format: :json)
